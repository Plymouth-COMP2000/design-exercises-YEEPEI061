<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center"
    android:layout_marginBottom="8dp"
    android:elevation="2dp"
    android:background="@drawable/input_bg">

    <ImageView
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:src="@drawable/ic_reservation_outline"
        android:contentDescription="@string/reservations"
        android:backgroundTint="@color/my_tertiary"
        android:padding="8dp"
        app:tint="@color/my_primary" />

    <TextView
        android:id="@+id/emptyHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No upcoming reservations!"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginTop="12dp"
        android:textColor="@color/my_primary"
        android:gravity="center"/>

    <TextView
        android:id="@+id/emptySubText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Looks like your calendar is clear."
        android:textSize="14sp"
        android:textColor="@color/my_medium"
        android:gravity="center"/>
</LinearLayout>
